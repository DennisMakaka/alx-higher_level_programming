-- Script to remove all records with a score <= 5 in the second_table of the hbtn_0c_0 database
-- Execute this script with the database name as an argument to the mysql command

DELETE FROM 'second table'
WHERE 'score' <= 5;
