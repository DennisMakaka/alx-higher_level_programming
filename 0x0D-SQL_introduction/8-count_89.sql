-- Script to display the number of records with id = 89 in the first_table of the hbtn_0c_0 database
-- Execute this script with the database name as an argument to the mysql command

SELECT COUNT(*)
FROM `first_table`
WHERE `id` = 89;
